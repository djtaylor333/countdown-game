package com.djtaylor.countdowngame.ui.theme

import androidx.compose.ui.graphics.Color

// ── Countdown brand colours ───────────────────────────────────────────────────
val Navy          = Color(0xFF1A2744)   // background
val NavyDark      = Color(0xFF0F1A3D)   // darker surface
val NavyDeep      = Color(0xFF090E21)   // darkest container
val Gold          = Color(0xFFF59E0B)   // primary accent (Countdown yellow-gold)
val GoldLight     = Color(0xFFFBBF24)   // lighter gold
val GoldDark      = Color(0xFFB45309)   // darker gold for light mode
val Cream         = Color(0xFFFEF3C7)   // warm off-white text on dark

// ── Letter tile colours ───────────────────────────────────────────────────────
val TileBlue      = Color(0xFF1E3A5F)   // default tile background
val TileBlueBorder = Color(0xFF2D5A8E)  // tile border
val TileSelected  = Color(0xFFF59E0B)   // selected/active tile (gold)
val TileUsed      = Color(0xFF374151)   // used letter tile

// ── Status colours ────────────────────────────────────────────────────────────
val Success       = Color(0xFF10B981)   // correct / valid (green)
val Error         = Color(0xFFEF4444)   // wrong / invalid (red)
val Warning       = Color(0xFFF59E0B)   // near miss (gold)
val TimerGreen    = Color(0xFF22C55E)   // timer abundant
val TimerAmber    = Color(0xFFF59E0B)   // timer warning
val TimerRed      = Color(0xFFEF4444)   // timer critical

// ── Number tile colours ───────────────────────────────────────────────────────
val NumberTileLarge  = Color(0xFF7C3AED) // large numbers (purple)
val NumberTileSmall  = Color(0xFF1E3A5F) // small numbers (blue)

// ── Neutral palette ───────────────────────────────────────────────────────────
val TextPrimary   = Color(0xFFF1F5F9)
val TextSecondary = Color(0xFF94A3B8)
val TextMuted     = Color(0xFF64748B)
val Divider       = Color(0xFF2D3748)
